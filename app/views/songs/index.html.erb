<h1>Songs List:</h1>
<table>
  <tbody>
  <th>
    <td>Song Name</td>
  </th>
    <% @songs.each do |song| %>
    <tr>
      <td><%= link_to song.title, song_path(song) %></td>
    </tr>
    <% end %>
  </tbody>
</table>
